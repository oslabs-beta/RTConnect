import SignalingChannel = require('../lib/server/server');

// check to make sure npm test is working
describe('is working', () => {
  it('show work', () => {
    expect(true).toBeTruthy();
  });
});

describe('SignalingChannel initializeConnection()', () => {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  beforeAll(() => {
    // Start websocket server
  });

  afterAll(() => {
    // Close websocket server
  });

});